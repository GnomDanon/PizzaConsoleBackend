const Router = require('express')
const router = new Router()
//const Router = require('./Router')
//const Router = require('./Router')
//const Router = require('./Router')
//const Router = require('./Router')

//router.use('/', Router)
//router.use('/', Router)
//router.use('/', Router)
//router.use('/', Router)

module.exports = router
